services:
  app:
    build: ./app
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
